{% extends 'base.html.twig' %}

{% block title %}Créer une discussion{% endblock %}

{% block body %}
    <h1>Créer une nouvelle discussion</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Créer</button>
    {{ form_end(form) }}

    <a href="{{ path('admin_discussion_list') }}" class="btn btn-secondary">Retour à la gestion des discussions</a>
{% endblock %}
